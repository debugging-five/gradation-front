import React from 'react';
import { Outlet } from 'react-router-dom';

const DisplayRegistrationContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default DisplayRegistrationContainer;