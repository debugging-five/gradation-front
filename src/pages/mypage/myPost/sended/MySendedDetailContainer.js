import React from 'react';
import { Outlet } from 'react-router-dom';

const MySendedDetailContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MySendedDetailContainer;