import React from 'react';
import { Outlet } from 'react-router-dom';

const MySendedListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MySendedListContainer;