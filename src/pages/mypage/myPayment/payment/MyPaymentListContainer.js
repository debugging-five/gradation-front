import React from 'react';
import { Outlet } from 'react-router-dom';

const MyPaymentListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MyPaymentListContainer;