import React from 'react';
import { Outlet } from 'react-router-dom';

const MyAvailableAuctionArtContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MyAvailableAuctionArtContainer;