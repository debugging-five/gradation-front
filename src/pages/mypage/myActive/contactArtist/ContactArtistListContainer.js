import React from 'react';
import { Outlet } from 'react-router-dom';

const ContactArtistListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default ContactArtistListContainer;