import React from 'react';
import { Outlet } from 'react-router-dom';

const ArtLikeListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default ArtLikeListContainer;