import React from 'react';
import { Outlet } from 'react-router-dom';

const UniversityLikeListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default UniversityLikeListContainer;