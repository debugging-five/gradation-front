import React from 'react';
import { Outlet } from 'react-router-dom';

const MypageDeleteContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MypageDeleteContainer;