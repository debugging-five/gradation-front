import React from 'react';
import { Outlet } from 'react-router-dom';

const ChangePasswordOkContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default ChangePasswordOkContainer;