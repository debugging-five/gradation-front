import React from 'react';
import { Outlet } from 'react-router-dom';

const UniversityCheckContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default UniversityCheckContainer;