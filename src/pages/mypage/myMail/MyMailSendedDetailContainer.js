import React from 'react';
import { Outlet } from 'react-router-dom';

const MyMailSendedDetailContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default MyMailSendedDetailContainer;