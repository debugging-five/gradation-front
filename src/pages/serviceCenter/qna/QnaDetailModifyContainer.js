import React from 'react';
import { Outlet } from 'react-router-dom';

const QnaDetailModifyContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default QnaDetailModifyContainer;