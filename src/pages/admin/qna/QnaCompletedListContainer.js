import React from 'react';
import { Outlet } from 'react-router-dom';

const QnaCompletedListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default QnaCompletedListContainer;