import React from 'react';
import { Outlet } from 'react-router-dom';

const QnaWaitingListContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default QnaWaitingListContainer;