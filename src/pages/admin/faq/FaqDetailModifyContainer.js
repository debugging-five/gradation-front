import React from 'react';
import { Outlet } from 'react-router-dom';

const FaqDetailModifyContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default FaqDetailModifyContainer;