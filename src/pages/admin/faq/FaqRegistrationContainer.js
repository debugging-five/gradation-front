import React from 'react';
import { Outlet } from 'react-router-dom';

const FaqRegistrationContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default FaqRegistrationContainer;