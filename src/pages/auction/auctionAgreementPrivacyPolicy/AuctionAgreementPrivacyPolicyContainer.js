import React from 'react';
import { Outlet } from 'react-router-dom';

const AuctionAgreementPrivacyPolicyContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default AuctionAgreementPrivacyPolicyContainer;