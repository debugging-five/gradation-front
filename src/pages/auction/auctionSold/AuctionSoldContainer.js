import React from 'react';
import { Outlet } from 'react-router-dom';

const AuctionSoldContainer = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default AuctionSoldContainer;